/**
 * Tokens service
 */
import { axios } from '../../libraries/utils'

function LoginService () {
}

LoginService.login = function (userName, password) {
  const url = ''
  return axios.get(url)
}

export default LoginService
